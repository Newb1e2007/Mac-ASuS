#include <bits/stdc++.h>

using namespace std;
using pii = pair<int, int>;

struct num {
    int a, b, c, d;
    bool canMake = false;
};

int n = 6561;
int INF = 1e9;

vector<int> dijkstra(num s, num f) {
    vector<vector<vector<vector<int>>>> dist(9, vector<vector<vector<int>>>(9), vector<vector<int>>(9));
    priority_queue<

}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);


    return 0;
}